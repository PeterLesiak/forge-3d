{
    "name": "@forge-3d/core",
    "type": "module",
    "scripts": {
        "build": "tspc && node esbuild.config.js && tspc -p tsconfig.build.json",
        "lint": "tspc"
    },
    "devDependencies": {
        "@forge-3d/internal": "workspace:^"
    }
}
